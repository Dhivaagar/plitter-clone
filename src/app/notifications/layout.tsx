export const metadata = {
  title: "Plitter - Notifications",
  icons: {
    icon: "/images/favicon.ico",
  },
};

const UserLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default UserLayout;
